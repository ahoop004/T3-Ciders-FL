seed: 27

baseline:
  dataset_path: ./Data/Imagenette
  dataset_name: Imagenette
  non_iid_per: 0.0
  num_clients: 50
  fraction_clients: 0.6
  num_rounds: 80
  num_epochs: 15
  batch_size: 96
  local_lr: 0.003
  global_lr: 1.0
  num_classes: 10
  criterion: torch.nn.CrossEntropyLoss

surrogate:
  dataset_path: ./Data/Imagenette
  dataset_name: Imagenette
  non_iid_per: 0.0
  num_clients: 50
  fraction_clients: 0.6
  num_rounds: 80
  num_epochs: 15
  batch_size: 96
  local_lr: 0.003
  global_lr: 1.0
  num_classes: 10
  criterion: torch.nn.CrossEntropyLoss
  pretrained: true
  client_id: 0

attack:
  type: pgd
  poison_rate: 0.25
  target_label: 0
  epsilon: 0.03137255
  step_size: 0.00784314
  iters: 10
  criterion: torch.nn.CrossEntropyLoss

attack_run:
  malicious_fraction: 0.2
  seed: 2024
